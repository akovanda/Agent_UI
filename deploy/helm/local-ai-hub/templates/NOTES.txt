Local AI Hub {{ .Chart.AppVersion }} is installed in namespace {{ .Release.Namespace }}.

Model PVC:
  {{ default (printf "%s-models" (include "local-ai-hub.fullname" .)) .Values.llama.storage.existingClaim }}

Inspect workloads:
  kubectl -n {{ .Release.Namespace }} get pods,svc,pvc

Import a model through the repository toolbox:
  ./hub k8s model-import gpt-oss-20b /path/to/gpt-oss-20b.gguf
  ./hub k8s model-import stheno-8b /path/to/Llama-3.1-8B-Stheno-v3.4.Q4_K_M.gguf

{{- if not .Values.ingress.enabled }}
Open WebUI port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "local-ai-hub.fullname" . }}-openwebui 8080:{{ .Values.openWebUI.service.port }}

SillyTavern port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "local-ai-hub.fullname" . }}-sillytavern 8000:{{ .Values.sillyTavern.service.port }}
{{- end }}
